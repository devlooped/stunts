<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <IsPackable>true</IsPackable>
    <Description>
      Provides run-time stunt generation using Castle DynamicProxy, for projects that 
      cannot use the compile-time stunt generation provided built-in Stunts, which 
      requires C# 9.0.
    </Description>
  </PropertyGroup>

  <ItemGroup>
    <Compile Remove="Stunt.*.*" />
    <None Include="Stunt.*.*" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Castle.Core" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Stunts\Stunts.csproj" Pack="false" />
  </ItemGroup>
  
  <ItemGroup>
    <!-- When not packed standalone, include the build targets/sources too.
         The project readme is the package readme, not a build asset. -->
    <None Remove="readme.md" Condition="'$(IsPackable)' != 'true'" />
    <None Update="@(None)" PackFolder="build\netstandard2.0" Condition="'$(IsPackable)' != 'true'" />
    <PackageFile Include="@(None)" PackFolder="buildTransitive\netstandard2.0" Condition="'$(IsPackable)' != 'true'" />
  </ItemGroup>

</Project>
